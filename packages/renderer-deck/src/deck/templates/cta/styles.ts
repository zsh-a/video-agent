export const ctaStyles = `.slide--cta .slide__content {
  align-content: center;
}

.slide--cta .slide__header {
  justify-items: center;
  text-align: center;
}

.cta-block__arrow {
  letter-spacing: 0.08em;
  opacity: 0.88;
}

.cta-block__glow {
  background: radial-gradient(ellipse 60% 55% at 50% 55%, color-mix(in srgb, var(--accent) 14%, transparent), transparent);
  inset: 0;
  pointer-events: none;
  position: absolute;
  z-index: 0;
}

.cta-block__label,
.cta-block__arrow {
  position: relative;
  z-index: 1;
}`
